{{- if .Values.enabled }}
{{- if .Values.hooks.postInstallCrs.enabled }}
{{- $createKserve := .Values.components.kserve.enabled }}
{{- $createAzureKE := and .Values.azure.enabled .Values.azure.kubernetesEngine.enabled }}
{{- $createCoreweaveKE := and .Values.coreweave.enabled .Values.coreweave.kubernetesEngine.enabled }}
{{- $createAwsKE := and .Values.aws.enabled .Values.aws.kubernetesEngine.enabled }}
{{- $createGateway := and .Values.components.kserve.enabled .Values.components.kserve.gateway.create }}
{{- $createMaas := .Values.components.maas.enabled }}
{{- $createMaasGateway := and $createMaas .Values.components.maas.gateway.create }}
{{- $gatewayTls := and (or $createGateway $createMaasGateway) .Values.gateway.tls.enabled }}
{{- $appNs := .Values.rhaiOperator.applicationsNamespace }}
{{- $issuerName := .Values.gateway.tls.issuerRef.name }}
{{- $issuerKind := .Values.gateway.tls.issuerRef.kind }}
{{- if or $createKserve $createMaas $createAzureKE $createCoreweaveKE $createAwsKE $createGateway $createMaasGateway }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rhai-post-install-hook
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rhai-on-xks-chart.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "0"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: rhai-post-install-hook
  labels:
    {{- include "rhai-on-xks-chart.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "0"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
rules:
  - apiGroups:
      - components.platform.opendatahub.io
    resources:
      - kserves
      - modelsasservices
    verbs:
      - get
      - create
      - patch
      - update
  - apiGroups:
      - infrastructure.opendatahub.io
    resources:
      - azurekubernetesengines
      - coreweavekubernetesengines
      - awskubernetesengines
    verbs:
      - get
      - create
      - patch
      - update
  {{- if $createGateway }}
  - apiGroups:
      - gateway.networking.k8s.io
    resources:
      - gateways
    verbs:
      - get
      - create
      - patch
      - update
  - apiGroups:
      - gateway.networking.k8s.io
    resources:
      - gatewayclasses
    verbs:
      - get
  - apiGroups:
      - apiextensions.k8s.io
    resources:
      - customresourcedefinitions
    verbs:
      - get
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - get
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - create
      - patch
      - update
  - apiGroups:
      - ""
    resources:
      - namespaces
    verbs:
      - get
      - create
      - patch
  - apiGroups:
      - cert-manager.io
    resources:
      - certificates
    verbs:
      - get
      - create
      - patch
      - update
  {{- if and $gatewayTls (eq $issuerKind "ClusterIssuer") }}
  - apiGroups:
      - cert-manager.io
    resources:
      - clusterissuers
    resourceNames:
      - {{ $issuerName }}
    verbs:
      - get
  {{- end }}
  {{- if $createMaasGateway }}
  - apiGroups:
      - operator.authorino.kuadrant.io
    resources:
      - authorinos
    verbs:
      - get
      - patch
  - apiGroups:
      - apps
    resources:
      - deployments
    verbs:
      - get
      - patch
  - apiGroups:
      - ""
    resources:
      - pods
      - pods/exec
    verbs:
      - get
      - list
      - create
  {{- end }}
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: rhai-post-install-hook
  labels:
    {{- include "rhai-on-xks-chart.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "0"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: rhai-post-install-hook
subjects:
  - kind: ServiceAccount
    name: rhai-post-install-hook
    namespace: {{ .Release.Namespace }}
{{- if $gatewayTls }}
---
# Certificate is namespaced to the applications namespace.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: rhai-post-install-hook-cert
  namespace: {{ $appNs }}
  labels:
    {{- include "rhai-on-xks-chart.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "0"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
rules:
  - apiGroups:
      - cert-manager.io
    resources:
      - certificates
    verbs:
      - get
      - create
      - patch
      - update
  {{- if eq $issuerKind "Issuer" }}
  - apiGroups:
      - cert-manager.io
    resources:
      - issuers
    resourceNames:
      - {{ $issuerName }}
    verbs:
      - get
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: rhai-post-install-hook-cert
  namespace: {{ $appNs }}
  labels:
    {{- include "rhai-on-xks-chart.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "0"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: rhai-post-install-hook-cert
subjects:
  - kind: ServiceAccount
    name: rhai-post-install-hook
    namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
